package Pattern;

import java.util.Arrays;
import java.util.Map;
import java.util.stream.Collectors;
import java.util.*;
public class StreamApi {
    //String arr[] = {"india", "nepal", "USA", "france", "india"};

    //Map<String, Integer> streamMap = Arrays.stream(arr).collect(Collectors.toMap(e->e, String::length));

   // Optional<String> res = Arrays.stream(arr).distinct().max(Comparator.comparingInt(String::length));
}
